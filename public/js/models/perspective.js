function Perspective(info) {
  this.title = info.title;
  this.date = info.date;
  this.longitude = info.longitude;
  this.latitude = info.latitude;
  this.text = info.text;
}

module.exports = Perspective;
